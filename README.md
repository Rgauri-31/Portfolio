# Gauri Rakhonde — Portfolio

Personal portfolio website showcasing my projects, certificates, and technical skills.

## 🚀 Live Demo

**Website:** [gaurisportfolio.vercel.app](https://gaurisportfolio.vercel.app)

## 🛠️ Tech Stack

- **ReactJS** — Frontend framework
- **Tailwind CSS** — Utility-first CSS framework
- **Supabase** — Backend for portfolio data, certificates & comment system
- **Framer Motion** — Animation library
- **AOS** — Animate On Scroll
- **Material UI** — Component library
- **Lucide** — Icon library
- **SweetAlert2** — Alert dialogs

## 📋 Prerequisites

- **Node.js** (v14.x or higher)
- **npm** or **yarn**

## 🏃‍♂️ Getting Started

### 1. Clone the Repository

```bash
git clone https://github.com/Rgauri-31/Portfolio.git
cd Portfolio/Portofolio_V5
```

### 2. Install Dependencies

```bash
npm install --legacy-peer-deps
```

### 3. Set Up Environment Variables

Create a `.env` file in the project root:

```env
VITE_SUPABASE_URL=your-supabase-url
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key
```

> **Note:** All env variables must be prefixed with `VITE_` for Vite. Restart the dev server after changes.

### 4. Run Development Server

```bash
npm run dev
```

Open [http://localhost:5173](http://localhost:5173) in your browser.

## 🏗️ Building for Production

```bash
npm run build
```

Build output is in the `dist` folder.

## ⚙️ Supabase Setup

### Database Tables

Run this SQL in your Supabase **SQL Editor**:

```sql
-- Projects table
CREATE TABLE public.projects (
  id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  created_at timestamp with time zone DEFAULT now() NOT NULL,
  "Title" text,
  "Description" text,
  "Img" text,
  "Link" text,
  "Github" text,
  "Features" jsonb,
  "TechStack" jsonb
);

-- Certificates table
CREATE TABLE public.certificates (
  id bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  created_at timestamp with time zone DEFAULT now() NOT NULL,
  "Img" text
);

-- Comments table
CREATE TABLE public.portfolio_comments (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  content TEXT NOT NULL,
  user_name VARCHAR(255) NOT NULL,
  profile_image TEXT,
  is_pinned BOOLEAN DEFAULT FALSE,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- Enable RLS
ALTER TABLE public.projects ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.certificates ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.portfolio_comments ENABLE ROW LEVEL SECURITY;

-- Public read policies
CREATE POLICY "Public read projects" ON public.projects FOR SELECT TO public USING (true);
CREATE POLICY "Public read certificates" ON public.certificates FOR SELECT TO public USING (true);
CREATE POLICY "Public read comments" ON public.portfolio_comments FOR SELECT TO public USING (true);
CREATE POLICY "Public insert comments" ON public.portfolio_comments FOR INSERT TO public WITH CHECK (is_pinned = false);

-- Storage bucket for comment profile images
INSERT INTO storage.buckets (id, name, public) VALUES ('profile-images', 'profile-images', true) ON CONFLICT (id) DO NOTHING;
CREATE POLICY "Upload profile images" ON storage.objects FOR INSERT TO public WITH CHECK (bucket_id = 'profile-images');
CREATE POLICY "Read profile images" ON storage.objects FOR SELECT TO public USING (bucket_id = 'profile-images');
```

Enable **Realtime** for the `portfolio_comments` table in Table Editor.

## 📞 Contact

**Gauri Rakhonde**
- GitHub: [Rgauri-31](https://github.com/Rgauri-31)
- LinkedIn: [gauri-rakhonde](https://www.linkedin.com/in/gauri-rakhonde-4482b52a2)
- Email: gaurirrakhonde@gmail.com

---

> Built with ❤️ · Based on [EkiZR/Portofolio_V5](https://github.com/EkiZR/Portofolio_V5) template
